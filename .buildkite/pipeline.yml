default: &default
  PROJECT_NAME: 'ItalianPlumber'
  SCHEME: 'ItalianPlumber'
  BUNDLE_ID: 'com.bszamody.ItalianPlumber'

steps:
  - label: 'Test Build'
    branches: 'code-climate'
    command: 'source ~/.bashrc && fastlane test'
    artifact_paths: 'xml_report'
    env:
      <<: *default
    agents:
      ios: 'build'
